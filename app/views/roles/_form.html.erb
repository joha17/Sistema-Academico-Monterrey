<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= form_for(@role) do |f| %>
        <% if @role.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@libro.errors.count, "error") %> Verifique que todos los datos esten completos:</h2>

            <ul>
            <% @libro.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
  <div class="field">
    <strong>Nombre del Rol</strong><br>
    <%= f.text_field :nomRole, autofocus: true, class: "form-control" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Aceptar", class: "btn btn-success" %>
  </div>
<% end %>
    </div>
  </div>
</div>
<br>
<br>
<br>
<br>
<br>
