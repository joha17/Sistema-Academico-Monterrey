<% if current_user %>
<% if can? :show, Item %>
<div class="row">
    <div class="col-sm-10 col-sm-offset-1">
      <p id="notice"><%= notice %></p>
      <div class="jumbotron">
        <div class="container">
          <p>
            <strong>Nombre:</strong>
            <%= @item.nomItem %>
          </p>

          <p>
            <strong>Descripcion:</strong>
            <%= @item.desItem %>
          </p>

          <p>
            <strong>Cantidad:</strong>
            <%= @item.canItem %>
          </p>

          <p>
            <strong>Usuario:</strong>
            <%= @item.user.NomUs %>
          </p>

          <p>
            <strong>Lugar:</strong>
            <%= @item.place.NomPlace %>
          </p>

          <p>
            <strong>Categoria:</strong>
            <%= @item.category.nomCategory %>
          </p>
          <br>

          <p><%= link_to "Ir al listado de Inventario", items_path %></p>
        </div>
      </div>
    </div>
  </div>

<%else%>
<div class="alert alert-danger" role="alert">No estas Autorizado</div>
<%end%>

<%else%>
  <div class="alert alert-danger" role="alert">No estas Autorizado</div>
<%end%>
