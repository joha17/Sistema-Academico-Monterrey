<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= form_for(@place) do |f| %>
        <% if @place.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@place.errors.count, "error") %> Verifique que todos los datos esten completos:</h2>

            <ul>
            <% @place.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <strong>Nombre del Lugar</strong><br>
          <%= f.text_field :NomPlace, autofocus: true, class: "form-control" %>
        </div>
        <br>
        <div class="actions text-center">
          <%= f.submit "Aceptar", class: "btn btn-success" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
