<% if current_user %>

<p id="notice"><%= notice %></p>

<h1>Listing Signaturas</h1>

<table>
  <thead>
    <tr>
      <th>Nomsig</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @signaturas.each do |signatura| %>
      <tr>
        <td><%= signatura.nomSig %></td>
        <td><%= link_to 'Show', signatura %></td>
        <td><%= link_to 'Edit', edit_signatura_path(signatura) %></td>
        <td><%= link_to 'Destroy', signatura, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Signatura', new_signatura_path %>

<%else%>
<div class="alert alert-danger" role="alert">No estas Autorizado</div>
  <%end%>
