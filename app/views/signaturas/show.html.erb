<% if current_user %>
<% if can? :show, Signatura %>
<p id="notice"><%= notice %></p>

<p>
  <strong>Nomsig:</strong>
  <%= @signatura.nomSig %>
</p>

<%= link_to 'Edit', edit_signatura_path(@signatura) %> |
<%= link_to 'Back', signaturas_path %>
<%else%>
  <div class="alert alert-danger" role="alert">No estas Autorizado</div>
<%end%>
<%else%>
  <div class="alert alert-danger" role="alert">No estas Autorizado</div>
<%end%>