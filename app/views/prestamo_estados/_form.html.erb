<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= form_for(@prestamo_estado) do |f| %>
        <% if @prestamo_estado.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@libro.errors.count, "error") %> Verifique que todos los datos esten completos:</h2>

            <ul>
            <% @prestamo_estado.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

  <div class="field">
    <strong>Nombre Prestamo</strong><br>
    <%= f.text_field :nomEstPres, autofocus: true, class: "form-control" %>
  </div><br>
  <div class="field">
    <strong>Descripcion Estado</strong><br>
    <%= f.text_area :desEstPres, autofocus: true, class: "form-control" %>
  </div>
  <br>
  <div class="text-center">
      <%= f.submit "Aceptar", class: "btn btn-primary" %>
  </div>
<% end %>
