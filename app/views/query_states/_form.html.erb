<%= form_for(@query_state) do |f| %>
  <% if @query_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query_state.errors.count, "error") %> prohibited this query_state from being saved:</h2>

      <ul>
      <% @query_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :DeState %><br>
    <%= f.text_field :DeState %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
